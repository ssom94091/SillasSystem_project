<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="contractDAO">

<!--로그인 -->
	<insert id="insertTemplate" parameterType="ContractVO">
		INSERT INTO contract_tb (
	  content
			
	)
	VALUES(
		#{content}
	)
</insert>

<select id="seletViewContract"  parameterType="String" resultType="egovMap">
	SELECT 
		content
	FROM contract_tb
	</select>
</mapper>