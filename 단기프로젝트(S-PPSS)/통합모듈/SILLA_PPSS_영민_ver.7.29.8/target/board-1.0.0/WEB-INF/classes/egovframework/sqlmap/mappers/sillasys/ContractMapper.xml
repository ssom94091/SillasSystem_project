<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ContractDAO">

	<insert id="trmTemplate1" parameterType="ContractVO">

		INSERT INTO contract_tb (
		admin_id
		, user_id
		, start_year
		, start_month
		, start_day
		, work_place
		, content
		, work_per_week
		, holiday
		, pay_type
		, pay
		, bonus
		, payday
		, payfor
		, em_insurance
		, ac_insurance
		, pension
		, hlt_insurance
		, canvas
		, contract_year
		, contract_month
		, contract_day
		, admin_name
		)
		VALUES(
		#{admin_id}
		, #{user_id}
		, #{start_year}
		, #{start_month}
		, #{start_day}
		, #{work_place}
		, #{content}
		, #{work_per_week}
		, #{holiday}
		, #{pay_type}
		, #{pay}
		, #{bonus}
		, #{payday}
		, #{payfor}
		, #{em_insurance}
		, #{ac_insurance}
		, #{canvas}
		, #{hlt_insurance}
		, #{sign-canvas}
		, #{contract_year}
		, #{contract_month}
		, #{contract_day}
		, #{admin_name}
		)
	</insert>
</mapper>